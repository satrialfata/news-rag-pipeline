version: "3.9"

services:
  news-rag-api:
    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: news-rag-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped